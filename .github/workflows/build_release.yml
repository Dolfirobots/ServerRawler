name: Release Build

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Build for ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            artifact_name: ServerRawler-Linux
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: ServerRawler-Windows.exe
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: ServerRawler-Apple

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install MUSL Tools (only for linux)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get install -y musl-tools

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename and Prepare Binary
        shell: bash
        run: |
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            BIN_PATH=$(find target/${{ matrix.target }}/release -maxdepth 1 -name "*.exe" | head -n 1)
          else
            BIN_PATH=$(find target/${{ matrix.target }}/release -maxdepth 1 -type f ! -name "*.*" | head -n 1)
          fi
          
          echo "Found binary: $BIN_PATH"
          cp "$BIN_PATH" ./${{ matrix.artifact_name }}

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifact_name }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}